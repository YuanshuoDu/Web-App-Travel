import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    title: {
        color: 'rgba(0,1,1,1)',
    },
    button: {
        color: 'white',
    },
    container: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    },
}));